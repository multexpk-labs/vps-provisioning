# Synthetic example only. Do not place production secrets here.
#cloud-config
package_update: true
packages:
  - curl
  - ca-certificates

runcmd:
  - [ sh, -c, "echo provisioning-complete > /var/tmp/provisioning-status" ]
